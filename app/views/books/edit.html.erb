<h2>Editing Book</h2>
<%= form_with model: @book do |f| %>
  <label for="book_title">Title</label>
  <%= f.text_field :book_name, autofocus: true %>
  <label for="book_opinion">Opinion</label>
  <%= f.text_area :caption, autofocus: true %>
  <%= f.submit "Update Book" %>
<% end %>